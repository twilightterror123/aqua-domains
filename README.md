# AQUA-Domains

Clean frontend for generating subdomain addresses for domains you control.

## Example

`deinapp.aqua.de`

## Vercel

This frontend does not create DNS records automatically. A real subdomain requires control of the base domain and DNS/Vercel configuration.

